name: Scheduled Job Search

on:
  schedule:
    - cron: '0 8 * * *'  # Runs daily at 08:00 AM UTC (03:00 AM CT)
  workflow_dispatch:      # Allows manual trigger

jobs:
  run-automator:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Configure Git
        run: |
          git config --global user.name 'Job Search Automator Bot'
          git config --global user.email 'bot@jobsearch.internal'

      - name: Run Automator
        env:
          APIFY_API_TOKEN: ${{ secrets.APIFY_API_TOKEN }}
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-2.5-flash
          SARVAM_API_KEY: ${{ secrets.SARVAM_API_KEY }}
          SARVAM_MODEL: sarvam-2b
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DATABASE_URL: sqlite:///data/jobs.db
          PYTHONPATH: .
          AUTO_APPLY_ENABLED: false
          USE_LOCAL_LLM: false
        run: python main.py

      - name: Push changes
        run: |
          git add -A
          git commit -m "Automated run data update [skip ci]" || echo "No changes to commit"
          git push
